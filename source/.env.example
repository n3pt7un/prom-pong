# CyberPong Environment Configuration

# ============================================
# Database Configuration
# ============================================

# Set to 'true' to use Supabase PostgreSQL instead of JSON file
# Leave empty or set to 'false' to use GCS/Local JSON file (legacy mode)
USE_SUPABASE=false

# Supabase Configuration (required if USE_SUPABASE=true)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Google Cloud Storage (optional - legacy mode or file storage)
# If not set, uses local filesystem
GCS_BUCKET=your-bucket-name

# ============================================
# Firebase Configuration
# ============================================

# Firebase Admin SDK uses Application Default Credentials by default
# Set GOOGLE_APPLICATION_CREDENTIALS if using a service account file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ============================================
# Admin Configuration
# ============================================

# Comma-separated list of emails that should be auto-promoted to admin
ADMIN_EMAILS=admin@example.com,another@example.com

# ============================================
# Server Configuration
# ============================================

PORT=8080
