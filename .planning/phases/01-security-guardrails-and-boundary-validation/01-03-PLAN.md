---
phase: 01-security-guardrails-and-boundary-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - source/server/__tests__/security-guardrails.test.ts
  - source/server/__tests__/validation-boundaries.test.ts
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - TEST-01
  - TEST-04
must_haves:
  truths:
    - Automated backend tests prove auth/admin boundary allow-deny behavior and malformed payload handling.
    - Deployment workflow blocks release when stabilization-critical backend security suites fail.
    - Security guardrails are regression-protected through repeatable CI checks.
  artifacts:
    - path: source/server/__tests__/security-guardrails.test.ts
      provides: Runtime guard and auth/admin boundary regression tests.
    - path: source/server/__tests__/validation-boundaries.test.ts
      provides: Centralized validation failure/success behavior tests.
    - path: .github/workflows/deploy.yml
      provides: Blocking pre-deploy test gate for stabilization-critical suites.
  key_links:
    - from: .github/workflows/deploy.yml
      to: source/server/__tests__/security-guardrails.test.ts
      via: mandatory CI step command
      pattern: npm test
    - from: .github/workflows/deploy.yml
      to: source/server/__tests__/validation-boundaries.test.ts
      via: mandatory CI step command
      pattern: npm test
    - from: source/server/__tests__/validation-boundaries.test.ts
      to: source/server/middleware/validate-request.js
      via: route-level validation assertions
      pattern: validateRequest
---

<objective>
Lock Phase 1 security behavior behind backend regression tests and enforce deployment blocking gates for these suites.

Purpose: Prevent accidental regression of auth/validation guardrails while remediation continues in later phases.
Output: Dedicated backend security tests and deploy workflow gating for phase-critical verification commands.
</objective>

<execution_context>
@/Users/tarasivaniv/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/tarasivaniv/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-guardrails-and-boundary-validation/01-CONTEXT.md
@source/jest.config.cjs
@.github/workflows/deploy.yml
@.planning/phases/01-security-guardrails-and-boundary-validation/01-01-PLAN.md
@.planning/phases/01-security-guardrails-and-boundary-validation/01-02-PLAN.md

<interfaces>
From source/jest.config.cjs:
```javascript
module.exports = {
  preset: 'ts-jest',
  testMatch: ['**/?(*.)+(spec|test).[jt]s?(x)']
}
```

From .github/workflows/deploy.yml:
```yaml
jobs:
  deploy:
    steps:
      - name: Checkout code
      - name: Setup Node.js
      - name: Build and push Docker image
      - name: Deploy to Cloud Run
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend tests for runtime guardrails and auth/admin boundaries</name>
  <files>source/server/__tests__/security-guardrails.test.ts</files>
  <behavior>
    - Test 1: Runtime guard logic rejects unsafe LOCAL_DEV + non-development combinations.
    - Test 2: Auth middleware preserves 401 behavior for missing/invalid bearer token.
    - Test 3: Admin middleware allows known admins and denies non-admin users.
  </behavior>
  <action>Create targeted Jest tests for runtime guard helper and auth/admin middleware boundary behavior, using focused mocks for req/res/next and dbOps/admin lookup dependencies. Cover allow and deny paths explicitly and keep assertions aligned with existing error payload messages.</action>
  <verify>
    <automated>cd source && npx jest server/__tests__/security-guardrails.test.ts --runInBand</automated>
  </verify>
  <done>Security guard and auth/admin boundary behavior is validated by deterministic backend tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend tests for centralized validation boundaries on high-risk routes</name>
  <files>source/server/__tests__/validation-boundaries.test.ts</files>
  <behavior>
    - Test 1: Malformed payloads for representative admin/matches/import mutations return 400 with code/details.
    - Test 2: Semantically invalid payloads return 422 where schema marks domain-level failures.
    - Test 3: Unknown fields are rejected and valid payloads pass through.
  </behavior>
  <action>Create route-level or middleware-level tests that exercise centralized validation behavior introduced in Plan 01-02. Use minimal Express harnesses/mocks to assert status code and response body contract. Keep tests focused on boundary guarantees, not deep business logic already covered elsewhere.</action>
  <verify>
    <automated>cd source && npx jest server/__tests__/validation-boundaries.test.ts --runInBand</automated>
  </verify>
  <done>Validation boundaries are regression-protected with explicit malformed/semantic/unknown-field coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Add blocking deploy gate for phase-critical backend security suites</name>
  <files>.github/workflows/deploy.yml</files>
  <action>Insert a required pre-deploy verification step in the deploy workflow that runs phase-critical backend tests and fails the job on any failure. Keep current deployment sequence intact but ensure image build/deploy only occurs after the verification step passes. Mark gate as mandatory for main branch deployment path with no bypass in workflow logic.</action>
  <verify>
    <automated>cd source && npm test -- --runInBand server/__tests__/security-guardrails.test.ts server/__tests__/validation-boundaries.test.ts</automated>
  </verify>
  <done>Deploy pipeline blocks promotion when phase-critical backend security/validation suites fail.</done>
</task>

</tasks>

<verification>
1. Run both dedicated security test files locally and confirm pass.
2. Validate deploy workflow now includes and requires the pre-deploy backend verification gate.
3. Simulate a failing test to confirm deploy workflow would stop before build/deploy stages.
</verification>

<success_criteria>
- Backend automated tests cover auth/admin boundaries and malformed payload handling for high-risk routes.
- Deploy workflow contains a mandatory security/regression gate before build/deploy.
- Security hardening changes from earlier plans are protected from silent regression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-guardrails-and-boundary-validation/01-03-SUMMARY.md`
</output>
