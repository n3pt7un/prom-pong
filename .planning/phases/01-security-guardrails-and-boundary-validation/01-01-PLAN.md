---
phase: 01-security-guardrails-and-boundary-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/server/config.js
  - source/server/index.js
  - source/server/middleware/auth.js
  - source/server/security/runtime-guards.js
  - source/server/security/csp-profile.js
autonomous: true
requirements:
  - SECU-04
  - SECU-05
must_haves:
  truths:
    - Development auth bypass only works in explicitly safe local-dev conditions.
    - Unsafe environment combinations fail before the server starts serving traffic.
    - CSP headers remain Firebase-popup compatible while introducing a staged hardening profile.
  artifacts:
    - path: source/server/security/runtime-guards.js
      provides: Centralized startup/runtime guard evaluation and fail-fast checks.
    - path: source/server/security/csp-profile.js
      provides: Staged CSP directives builder for compatibility-first hardening.
    - path: source/server/index.js
      provides: Startup guard invocation and Helmet CSP wiring through shared profile.
    - path: source/server/middleware/auth.js
      provides: Auth bypass gate uses shared guard conditions instead of ad-hoc checks.
  key_links:
    - from: source/server/index.js
      to: source/server/security/runtime-guards.js
      via: startup validation before app.listen
      pattern: validateRuntimeGuardrails
    - from: source/server/middleware/auth.js
      to: source/server/security/runtime-guards.js
      via: bypass allow condition
      pattern: canUseLocalDevBypass
    - from: source/server/index.js
      to: source/server/security/csp-profile.js
      via: helmet contentSecurityPolicy directives
      pattern: buildCspDirectives
---

<objective>
Implement runtime safety rails for dev bypass and staged CSP hardening without breaking existing auth popup/session behavior.

Purpose: Enforce explicit trust boundaries in server startup/auth middleware so unsafe env mixes cannot silently run.
Output: Reusable runtime guard and CSP profile modules integrated in server bootstrap/auth middleware.
</objective>

<execution_context>
@/Users/tarasivaniv/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/tarasivaniv/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-guardrails-and-boundary-validation/01-CONTEXT.md
@source/server/index.js
@source/server/middleware/auth.js
@source/server/config.js

<interfaces>
From source/server/middleware/auth.js:
```javascript
export const authMiddleware = async (req, res, next) => { ... }
export const adminMiddleware = async (req, res, next) => { ... }
```

From source/server/index.js:
```javascript
app.use(helmet({ ... }))
app.listen(PORT, '0.0.0.0', () => { ... })
```

From source/server/config.js:
```javascript
export const PORT = process.env.PORT || 8080;
export const GCS_BUCKET = process.env.GCS_BUCKET;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create explicit runtime guardrail contract for local-dev bypass</name>
  <files>source/server/security/runtime-guards.js, source/server/config.js</files>
  <behavior>
    - Test 1: Returns bypass-allowed only when NODE_ENV=development, LOCAL_DEV=true, and production indicators are absent.
    - Test 2: Returns startup-failure result when LOCAL_DEV=true appears in non-development environments.
    - Test 3: Exposes structured diagnostic values for startup warning logs.
  </behavior>
  <action>Create a shared guard helper module with functions that evaluate bypass eligibility and startup safety. Include explicit checks for NODE_ENV, LOCAL_DEV flag, and production indicators (including GCS_BUCKET and common deployment envs). Keep behavior-preserving 401 semantics untouched in auth middleware; only replace bypass condition source. Avoid duplicating inline env logic in multiple files.</action>
  <verify>
    <automated>cd source && node --check server/security/runtime-guards.js && node --check server/config.js</automated>
  </verify>
  <done>Guard helper exports deterministic pass/fail evaluation and config exposes values needed for startup diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate startup fail-fast and auth bypass observability</name>
  <files>source/server/index.js, source/server/middleware/auth.js</files>
  <behavior>
    - Test 1: Startup aborts with explicit error when unsafe LOCAL_DEV combination is detected.
    - Test 2: Startup logs a prominent warning when bypass mode is active in valid local-dev context.
    - Test 3: Non-bypass auth failures still return existing messages (Authentication required / Invalid or expired token).
  </behavior>
  <action>Call runtime guard validation before loading DB/listening. If invalid, log actionable details and exit non-zero. In auth middleware, gate bypass through shared helper and keep established response body/status behavior for standard auth failures. Include one clear startup warning line summarizing resolved guard values whenever bypass is active.</action>
  <verify>
    <automated>cd source && node --check server/index.js && node --check server/middleware/auth.js</automated>
  </verify>
  <done>Unsafe env combinations cannot start the API process, and safe bypass mode is explicit/observable in logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Externalize staged CSP profile and wire Helmet to compatibility-first baseline</name>
  <files>source/server/security/csp-profile.js, source/server/index.js</files>
  <behavior>
    - Test 1: Generated directives include required Firebase popup/auth origins for script-src and frame-src.
    - Test 2: Profile supports staged hardening toggle to remove unsafe-eval when enabled.
    - Test 3: Existing auth popup flow compatibility directives remain present in default profile.
  </behavior>
  <action>Create a CSP profile builder module and move directive construction out of server bootstrap. Default to compatibility-first profile that preserves Firebase popup flow; include a staged hardening switch for tighter profile rollout without forcing immediate breakage. Do not remove required Firebase origins or crossOriginOpenerPolicy override in this phase.</action>
  <verify>
    <automated>cd source && node --check server/security/csp-profile.js && node --check server/index.js</automated>
  </verify>
  <done>CSP setup is centralized and staging-ready, with default behavior preserving required auth popup/callback flow.</done>
</task>

</tasks>

<verification>
1. Start server with safe local-dev flags and confirm startup warning is emitted when bypass is active.
2. Start server with unsafe LOCAL_DEV combination and confirm process exits before listening.
3. Inspect response headers on a representative API route to confirm CSP directives are served from shared profile.
</verification>

<success_criteria>
- Dev bypass requires explicit safe local-dev conditions and cannot accidentally activate in non-dev environments.
- Unsafe env configuration fails fast at startup with clear diagnostics.
- CSP profile is staged and compatibility-first, preserving Firebase popup login flow requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-guardrails-and-boundary-validation/01-01-SUMMARY.md`
</output>
