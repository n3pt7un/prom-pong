---
phase: 01-security-guardrails-and-boundary-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - source/server/middleware/validate-request.js
  - source/server/validation/schemas.js
  - source/server/routes/admin.js
  - source/server/routes/matches.js
  - source/server/routes/export-import.js
autonomous: true
requirements:
  - SECU-03
must_haves:
  truths:
    - Invalid payloads on targeted mutating/admin routes return consistent 4xx errors before business logic executes.
    - Admin/mutation endpoints reject unknown fields rather than silently ignoring unsafe keys.
    - Error payloads follow one shape with machine-readable code and field-level details.
  artifacts:
    - path: source/server/middleware/validate-request.js
      provides: Shared route middleware for schema-based payload validation.
    - path: source/server/validation/schemas.js
      provides: High-risk route schemas for /api/admin/*, /api/matches, and /api/import.
    - path: source/server/routes/admin.js
      provides: Validation middleware applied to representative admin mutations.
    - path: source/server/routes/matches.js
      provides: Validation middleware applied to POST/PUT match mutations.
    - path: source/server/routes/export-import.js
      provides: Validation middleware applied to import/reset mutation boundaries.
  key_links:
    - from: source/server/routes/admin.js
      to: source/server/middleware/validate-request.js
      via: middleware chain before handler
      pattern: validateRequest
    - from: source/server/routes/matches.js
      to: source/server/validation/schemas.js
      via: schema binding for request body
      pattern: matches
    - from: source/server/routes/export-import.js
      to: source/server/validation/schemas.js
      via: import/reset schema usage
      pattern: import
---

<objective>
Add centralized payload validation across high-risk mutating and admin routes with consistent error contract and strict unknown-field rejection.

Purpose: Prevent partial execution or silent key filtering on invalid payloads while keeping route auth/authorization behavior stable.
Output: Shared validation middleware and schema definitions wired to priority high-risk routes.
</objective>

<execution_context>
@/Users/tarasivaniv/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/tarasivaniv/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-guardrails-and-boundary-validation/01-CONTEXT.md
@source/server/routes/admin.js
@source/server/routes/matches.js
@source/server/routes/export-import.js

<interfaces>
From source/server/routes/admin.js:
```javascript
router.put('/admin/users/:playerId', authMiddleware, adminMiddleware, async (req, res) => { ... })
router.post('/admin/admins', authMiddleware, adminMiddleware, async (req, res) => { ... })
router.post('/admin/leagues', authMiddleware, adminMiddleware, async (req, res) => { ... })
```

From source/server/routes/matches.js:
```javascript
router.post('/matches', authMiddleware, async (req, res) => { ... })
router.put('/matches/:id', authMiddleware, async (req, res) => { ... })
```

From source/server/routes/export-import.js:
```javascript
router.post('/import', express.json({ limit: '5mb' }), authMiddleware, adminMiddleware, async (req, res) => { ... })
router.post('/reset', authMiddleware, adminMiddleware, async (req, res) => { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define centralized validation contract and high-risk schemas</name>
  <files>source/server/middleware/validate-request.js, source/server/validation/schemas.js</files>
  <behavior>
    - Test 1: Malformed payload shape returns 400 with { error, code, details } format.
    - Test 2: Semantically invalid-but-well-formed payload returns 422 with same error shape.
    - Test 3: Unknown body keys are rejected on admin/mutating schemas.
  </behavior>
  <action>Create validation middleware and schema catalog using repository-compatible JavaScript module style. Standardize error response contract as { error, code, details } and support both 400 (shape/type) and 422 (domain semantics). Configure schemas with strict unknown-key rejection for admin and mutation paths. Do not change existing auth middleware ordering or success response payloads.</action>
  <verify>
    <automated>cd source && node --check server/middleware/validate-request.js && node --check server/validation/schemas.js</automated>
  </verify>
  <done>Reusable validation layer exists with strict schema definitions for prioritized routes and standard error payload shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply validation middleware to high-risk admin and match mutation routes</name>
  <files>source/server/routes/admin.js, source/server/routes/matches.js</files>
  <behavior>
    - Test 1: Invalid admin mutation payloads fail before dbOps mutation calls.
    - Test 2: Invalid match payloads fail with consistent 4xx contract.
    - Test 3: Valid payloads continue existing success behavior.
  </behavior>
  <action>Wire validateRequest middleware in front of handler functions for representative high-risk endpoints in admin and matches routes. Keep existing endpoint paths and auth/admin middleware intact, then remove duplicated ad-hoc shape checks where schema validation supersedes them. Preserve route-specific domain checks that cannot be captured cleanly in schema alone.</action>
  <verify>
    <automated>cd source && node --check server/routes/admin.js && node --check server/routes/matches.js</automated>
  </verify>
  <done>Targeted admin and match mutating endpoints consistently enforce schema validation with unified 4xx format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply validation on import/reset mutation boundary with strict unknown-key policy</name>
  <files>source/server/routes/export-import.js</files>
  <behavior>
    - Test 1: /api/import rejects malformed payloads and unknown keys with 4xx validation response.
    - Test 2: /api/reset enforces expected mode values and payload constraints.
    - Test 3: Valid import/reset payloads preserve current success behavior.
  </behavior>
  <action>Attach shared validation middleware to import/reset mutations and align current in-handler checks to centralized schema decisions. Keep import body-size middleware and auth/admin middleware chain unchanged. Ensure validation failures short-circuit before any state mutation call.</action>
  <verify>
    <automated>cd source && node --check server/routes/export-import.js</automated>
  </verify>
  <done>Import/reset boundaries enforce centralized strict validation with no partial execution on bad payloads.</done>
</task>

</tasks>

<verification>
1. Execute malformed payload requests against representative /api/admin/*, /api/matches, and /api/import endpoints and confirm consistent 4xx shape.
2. Confirm unknown payload keys are rejected for these routes.
3. Confirm valid requests still execute successfully.
</verification>

<success_criteria>
- Centralized validation is used on high-risk mutating/admin routes.
- Validation failures produce deterministic 4xx responses with { error, code, details } shape.
- Unknown field rejection is active on admin and mutation schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-guardrails-and-boundary-validation/01-02-SUMMARY.md`
</output>
