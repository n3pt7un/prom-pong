---
phase: 04-backend-read-path-and-recompute-isolation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - source/server/__tests__/reconciliation.test.mjs
  - source/server/__tests__/persistence-debounce.test.mjs
  - source/server/__tests__/recompute-isolation.test.mjs
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Tests for reconciliation guard, persistence debounce, and recompute isolation exist and fail (red) before any implementation"
    - "Each test file covers the behavior contract its corresponding implementation plan will satisfy"
    - "Running the Jest suite with passWithNoTests does not error due to missing files"
  artifacts:
    - path: "source/server/__tests__/reconciliation.test.mjs"
      provides: "Concurrency guard behavior contract for PERF-01"
      exports: []
    - path: "source/server/__tests__/persistence-debounce.test.mjs"
      provides: "Async debounce write contract for PERF-02"
      exports: []
    - path: "source/server/__tests__/recompute-isolation.test.mjs"
      provides: "Chunked recompute output-parity and concurrency-rejection contract for PERF-03"
      exports: []
  key_links:
    - from: "source/server/__tests__/reconciliation.test.mjs"
      to: "source/server/services/reconciliation.js"
      via: "import (file does not exist yet — tests must fail)"
      pattern: "import.*reconciliation"
    - from: "source/server/__tests__/persistence-debounce.test.mjs"
      to: "source/server/db/persistence.js"
      via: "import saveDB"
      pattern: "import.*persistence"
    - from: "source/server/__tests__/recompute-isolation.test.mjs"
      to: "source/server/services/reconciliation.js"
      via: "import runRecompute / isRecomputeInProgress"
      pattern: "import.*reconciliation"
---

<objective>
Create failing test scaffolds that define the behavioral contracts for PERF-01, PERF-02, and PERF-03 before any implementation begins.

Purpose: The Nyquist Rule requires automated test commands in every implementation task. All three implementation plans reference test files that do not yet exist. This Wave 1 plan creates those files with complete, failing test cases so that Wave 2 implementation plans can run against them immediately.

Output: Three `.mjs` test files under `source/server/__tests__/`, each with concrete, failing `it()` blocks that implementation must satisfy.
</objective>

<execution_context>
@/Users/tarasivaniv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarasivaniv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-read-path-and-recompute-isolation/04-CONTEXT.md
@.planning/phases/04-backend-read-path-and-recompute-isolation/04-RESEARCH.md
</context>

<interfaces>
<!-- Existing test file pattern from Phase 1 — executor should follow the same structure. -->
<!-- From source/server/__tests__/security-guardrails.test.mjs (established pattern): -->
<!-- - Uses .mjs extension -->
<!-- - Uses jest.mock() with ESM-compatible patterns -->
<!-- - Uses describe/it/expect -->
<!-- - jest.config.cjs server project matches *.mjs -->
<!-- Run command: cd source && NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern='server' --passWithNoTests -->

<!-- Target modules the tests will import (none exist yet — imports must fail): -->
<!-- source/server/services/reconciliation.js exports: runReconciliation, isReconciling, isRecomputeInProgress, runRecompute -->
<!-- source/server/db/persistence.js exports (existing): saveDB, getDB, loadDB -->
<!-- source/server/db/operations.js exports (existing): dbOps object with recalculateElo() method -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reconciliation service test scaffold (PERF-01)</name>
  <files>source/server/__tests__/reconciliation.test.mjs</files>
  <behavior>
    - Test: runReconciliation() sets reconciling flag, awaits expiry logic, clears flag in finally
    - Test: concurrent call while reconciling=true returns immediately (skips the loop body)
    - Test: per-match error in the expiry loop is caught per-iteration; loop continues; function does not throw
    - Test: isReconciling() returns true while runReconciliation() is active, false after completion
    - Test: isRecomputeInProgress() returns false by default
    - Test: runRecompute(dbOps) sets isRecomputeInProgress true during execution, false after
    - Test: concurrent runRecompute() call while one is active throws an error (message: 'Recompute already in progress')
    - Test: runRecompute() clears the flag even when the inner task throws (finally guarantee)
  </behavior>
  <action>
    Create `source/server/__tests__/reconciliation.test.mjs` with Jest test cases covering the reconciliation service contract.

    The file MUST import from `../../server/services/reconciliation.js` — this module does not exist yet, so all tests must fail at import or at execution time.

    Use jest.fn() to mock the expiry loop behavior passed to the service. Write concrete it() blocks for all eight behaviors listed above. Do not write implementation — only the test expectations.

    File structure:
    - `describe('runReconciliation')`: concurrency guard tests (4 cases)
    - `describe('runRecompute')`: recompute flag and concurrency guard tests (4 cases)

    CRITICAL — set the flag before any await. Tests must assert this ordering by checking the flag is true mid-execution, not just after.

    Path assumed by test: `../../server/services/reconciliation.js` relative to `__tests__/`.
  </action>
  <verify>
    <automated>cd /Users/tarasivaniv/Documents/test-pong/source && NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern='reconciliation' --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests either fail with import error (module not found) or with assertion failures. Zero passing tests is acceptable — test scaffolds must be red at this stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create persistence debounce test scaffold (PERF-02)</name>
  <files>source/server/__tests__/persistence-debounce.test.mjs</files>
  <behavior>
    - Test: saveDB() on GCS_BUCKET=truthy path does NOT call fs.writeFileSync (existing behavior preserved)
    - Test: saveDB() on local path does NOT call fs.writeFileSync (the synchronous variant must be absent)
    - Test: saveDB() on local path calls fs.promises.writeFile (or fs.promises equivalent) asynchronously
    - Test: calling saveDB() twice in rapid succession results in fewer than two actual fs.promises.writeFile calls (debounce coalescing)
    - Test: saveDB() does not throw; errors from fs.promises.writeFile are caught and logged via console.error
  </behavior>
  <action>
    Create `source/server/__tests__/persistence-debounce.test.mjs` with Jest test cases covering the debounced async write contract.

    The file imports `saveDB` from `../../server/db/persistence.js` (this file EXISTS — be careful not to break the import).

    Mock `fs` using `jest.mock('fs', ...)` and `jest.useFakeTimers()` to control debounce timing. Use `jest.advanceTimersByTime(600)` to trigger the debounce window after rapid calls.

    IMPORTANT: The current `saveDB` uses `fs.writeFileSync` on the local path. These tests are deliberately written to the target behavior, which means they will FAIL against the current implementation — that is the desired red state.

    File structure:
    - `describe('saveDB — local path')`: async write and debounce coalescing tests
    - `describe('saveDB — GCS path')`: verify GCS branch is not debounced or changed
  </action>
  <verify>
    <automated>cd /Users/tarasivaniv/Documents/test-pong/source && NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern='persistence-debounce' --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests fail against current implementation (writeFileSync is still present). Zero passing tests is acceptable at this stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create recompute isolation test scaffold (PERF-03)</name>
  <files>source/server/__tests__/recompute-isolation.test.mjs</files>
  <behavior>
    - Test: recalculateElo() on a known fixture of N matches produces the same final player ELO values as the current unbounded implementation (output parity regression guard)
    - Test: recalculateElo() called via runRecompute() while another runRecompute() is in-flight throws with message 'Recompute already in progress'
    - Test: recalculateElo() does not monopolize the event loop synchronously — setImmediate is called at least once during processing of > CHUNK_SIZE matches (yielding check)
    - Test: POST /admin/recalculate-elo returns 409 when recomputeInProgress is true
    - Test: GET /state response includes recomputeInProgress: true when isRecomputeInProgress() returns true
  </behavior>
  <action>
    Create `source/server/__tests__/recompute-isolation.test.mjs` with Jest test cases covering PERF-03 behavior.

    The file imports:
    - `runRecompute`, `isRecomputeInProgress` from `../../server/services/reconciliation.js` (does not exist yet — tests will fail)
    - `dbOps` from `../../server/db/operations.js` (exists)

    For the output-parity test: build a small fixture of 5-10 known matches with known winners/losers, run `dbOps.recalculateElo()` directly, and assert the final playerMap ELO values match hand-computed expected values. This test must PASS even before chunking (confirms no regression after chunking is added).

    For the setImmediate yielding test: spy on `setImmediate` and assert it was called at least once when processing a fixture with more than 50 matches.

    For the admin route 409 test: use supertest against the admin router with `isRecomputeInProgress` mocked to return true.

    For the GET /state `recomputeInProgress` test: use supertest against the state router with `isRecomputeInProgress` mocked to return true; assert response body includes `recomputeInProgress: true`.

    File structure:
    - `describe('recalculateElo — output parity')`: regression guard
    - `describe('runRecompute — concurrency guard')`: concurrent call rejection
    - `describe('recalculateElo — event loop yielding')`: setImmediate spy check
    - `describe('POST /admin/recalculate-elo — 409 guard')`: admin route conflict response
    - `describe('GET /state — recomputeInProgress flag')`: state route flag surfacing
  </action>
  <verify>
    <automated>cd /Users/tarasivaniv/Documents/test-pong/source && NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern='recompute-isolation' --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests that depend on reconciliation.js fail (module not found). Output-parity test may pass against current implementation — that is correct and expected. Overall: mostly red, output-parity case green is acceptable.</done>
</task>

</tasks>

<verification>
All three test files exist:
- source/server/__tests__/reconciliation.test.mjs
- source/server/__tests__/persistence-debounce.test.mjs
- source/server/__tests__/recompute-isolation.test.mjs

Run: `cd /Users/tarasivaniv/Documents/test-pong/source && NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern='reconciliation|persistence-debounce|recompute-isolation' --passWithNoTests 2>&1 | grep -E 'Tests:|Test Suites:|PASS|FAIL'`

Expected: test suites are found and run (even if all fail). No "Cannot find module" errors for persistence.js or operations.js (those exist). "Cannot find module" for reconciliation.js is expected and correct.
</verification>

<success_criteria>
- Three test scaffold files written and syntactically valid
- Tests are red (failing) for behaviors that do not yet exist
- Output-parity test for recalculateElo may be green (tests the current unbounded implementation, confirms baseline)
- No test file produces a syntax error or crashes Jest itself
- Wave 2 implementation plans can reference these test files in their `<verify>` blocks without creating them first
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-read-path-and-recompute-isolation/04-01-SUMMARY.md` following the summary template.
</output>
